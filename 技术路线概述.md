# 上下文阶梯 ContextLadder —— 技术路线概述

## 0. 命名：为什么叫「阶梯」

本方案把模型对实验条件的依赖组织成一道**可以逐级回退的阶梯**，两个核心设计都是这一原则在不同分支上的实例：

| 分支 | 阶梯结构 | 遇到未见实体时的回退 |
|---|---|---|
| 测量上下文 | 来源 → 仪器 → 板（板是仪器/来源之上的收缩残差，分支权重 0.25） | 未见板自动退回仪器/来源这一级，而不是落到无意义的随机嵌入 |
| 化学扰动 | 化合物 ID → 生物/测量上下文先验 | 训练时 25% 的化合物 token 被掩码为 `<UNK>`，迫使模型在未见化合物上退回上下文先验，而不是凭陌生 ID 硬编出扰动信号 |

一句话概括这条原则：**宁可退到更粗的一级，也不要在没有证据的地方假装知道。** 这正对应赛题的四个泛化场景——S1 未见化合物、S2 未见菌株、S3 双重未知、test_time 时间插值，每一个都要求模型在实体缺失时给出稳定而非任意的输出。

## 1. 总体流程

```text
官方 train_val 元数据与蛋白标签
            │
            ├── 数据审计、字段映射、样本/蛋白列对齐
            │
            ├── 按 split_final 切出 train（5,920 行）；val（3,038 行）仅用于模型选择
            │
            ├── train-only 蛋白筛选（缺失率 < 0.8 → 4,422 列）
            │
            ├── train-only 预处理器拟合
            │      ├── 六类类别字段 unknown-safe 映射（化合物/菌株/培养基/来源/仪器/板）
            │      ├── 温度/时间标准化 + log-时间 + 5×RBF 连续时间基
            │      │
            │      ├── 化学分支 ──┐
            │      ├── 生物分支 ──┼── HCCE FiLM 专家 ──┐
            │      └── 测量分支 ──┘（板=来源/仪器收缩残差）│
            │                                          ├─ 0.5×传统FiLM + 0.5×HCCEFiLM
            │      └── 传统 FiLM 专家（one-hot + FiLM）──┘
            │
            ├── A2 训练增强：随机 25% 化合物 token 掩码为 <UNK>
            │
            ├── 三种子（20260810 / 3407 / 42）各在 train 划分训练 → 等权集成
            │
            └── 4,422 蛋白 log2 预测（复赛提交口径的标准建模空间）
                          │
                          └── prediction.csv：4,454 行 × (sample_ID + 4,422 列)（格式校验）
```

## 2. 特征构造

### 2.1 类别输入（unknown-safe）

化合物、菌株、培养基、数据来源、仪器、板六个类别字段使用仅由训练数据构建的类别映射；未见类别统一落入保留的 `<UNK>` 槽位，保证 OOD 划分（未见化合物/未见菌株）下的推理稳定。

### 2.2 连续输入

温度与扰动时间经训练统计量标准化；时间维度额外使用 log-时间与 5 个 RBF 基构成的连续时间表示，刻画时间插值与时间外推场景。

### 2.3 测量上下文

来源/仪器/板构成层级化测量分支：板效应建模为来源与仪器之上的收缩残差（plate 分支权重 0.25），为未见板提供回退，缓解对 plate 快捷路径的过拟合。

## 3. 模型结构

最终候选模型 HCCE-Proteome（A2 掩码训练版）为双专家网络，提交输出为三种子等权集成：

1. **HCCE FiLM 专家**：化学分支、生物分支（菌株+培养基+数值条件）、测量分支（来源+仪器+板残差）分路编码；生物/测量上下文经 FiLM 调制化学表示，并保留 5% 权重上下文直连通路。
2. **传统 FiLM 专家**：高容量 one-hot 路线（化学 one-hot + 上下文 one-hot + FiLM 调制 + 5% 直连通路），保留精确 ID 记忆能力。
3. **A2 掩码实体训练**：训练时随机 25% 的化合物 token 掩码为 `<UNK>`（仅掩码化合物；双字段掩码经实验证实为负结果）。机制：防止化学分支对未见实体输出任意扰动信号，未见菌株与未见化合物子集同时受益。
4. **融合与集成**：每个种子内最终预测 = 0.5 × 传统 FiLM 专家 + 0.5 × HCCE FiLM 专家；三个种子（20260810、3407、42）等权平均。融合权重由 train-only 三种子/多类 OOF 证据选定，提交阶段固定不变。
5. **解码器**：多输出 MLP 一次预测全部保留蛋白目标；蛋白维度经 train-only 缺失率 < 0.8 筛选为 4,422，即复赛提交说明规定的标准建模空间；提交产物只含这 4,422 列。

模型使用 GELU 激活和 Dropout，优化器为 AdamW（plate 嵌入附加收缩正则），训练损失为蛋白目标标准化空间中的均方误差，最终预测再还原到 `log2(raw)` 空间。

## 4. 训练与提交流程

1. 读取官方 `train_val` 元数据和蛋白矩阵，对齐样本 ID 与蛋白列。
2. 仅用 `split_final == "train"` 划分（5,920 行）拟合蛋白保留阈值、类别映射、数值标准化、目标统计量与填充均值。
3. 在冻结验证协议（train-only OOF：compound/strain/both/time/plate 五类折叠）上确定专家融合与超参数；验证划分（3,038 行）只用于此步的指标计算。
4. 固定模型配置：40 轮、embedding 维度 64、掩码率 0.25、50/50 固定融合。
5. 在**同一 `train` 划分**上按三个种子（20260810、3407、42）各重训一次并等权集成；验证划分不参与重训。
6. 读取测试元数据，生成全部测试样本预测；821 个剔除蛋白以 `train` 划分均值填充（189 个训练内无观测的蛋白回退为 `train` 全局均值 21.5222）。
7. 写出 `prediction.csv`（`sample_ID` + 4,422 列），并检查样本 ID、行顺序、蛋白列、输出空间和有限值。
8. 可选：运行 `scripts/validate_submission.py` 独立复核提交格式与训练数据边界（无需重跑训练）。

## 5. 代码入口

- 最终训练入口：`scripts/train.py`（三 seed，落盘 `runs/final/checkpoints/`）。
- 最终推理入口：`scripts/predict.py`（只读 test metadata 与 run 目录）。
- 外部特征构建：`scripts/build_embeddings.py`（本方案无需执行）。
- 冻结的最终配置：`configs/final.yaml`。
- 初赛历史入口：`scripts/make_mask_compound_3seed_submission.py`（合体，产出 5,243 列，口径已弃用）。
- 掩码训练变体与训练流程：`scripts/a2b_train_variants.py`。
- HCCE-Proteome 基础模型与训练流程：`scripts/run_hcce.py`。
- 历史 HCCE 50/50 单种子提交入口（基线）：`scripts/make_hcce_submission.py`。
- 传统 FiLM 单模型入口（备选）：`scripts/make_submission.py`、`scripts/run_p5_interactions.py`。
- 基线模型：`scripts/run_baselines.py`。
- 数据审计：`scripts/p0_audit_data.py`。
- 提交格式与数据边界独立复核：`scripts/validate_submission.py`。
- 数据路径：`configs/data_paths.yaml`。
- 字段映射：`configs/field_mapping.yaml`。
- 蛋白列契约：`configs/protein_feature_contract.json`。

### 5.1 分析与审计工具（对应《方案说明文档》第十二节）

- 官方口径六模块评估器：`scripts/evaluate_official_modules.py`。
- 化合物不变性恒等式检查：`scripts/compound_invariance_check.py`。
- Gate-W 加权分汇总：`scripts/gate_w_summary.py`。
- 嵌套无泄漏 LOCO harness：`scripts/dcb_loco_harness.py`，其共享缓存由 `scripts/dcb_build_cache.py` 构建。
- 各阶段冻结判据：`configs/dcb_gates.json`、`configs/p05_gates.json`、`configs/p1_gates.json`、`configs/p3_gates.json`；冻结候选清单：`configs/m1_candidates.json`、`configs/p05_candidates.json`、`configs/p3_candidates.json`。
- LOCO harness 所需的派生外部特征：`external_data/processed/`（来源与版本见 `external_data/RAW_SOURCES.md`）。

## 6. 运行命令

```bash
cd <代码包根目录>
conda activate tl   # 或 pip install -r requirements.txt

python scripts/build_embeddings.py --output artifacts/embeddings          # 本方案无需执行
python scripts/train.py --metadata "input/WAYB_WAYC_metadata_train_val(1).csv" \
    --proteome input/WAYB_WAYC_proteome_raw_train_val.csv \
    --config configs/final.yaml --output-dir runs/final                   # 约 100 秒
python scripts/predict.py --metadata "input/WAYB_WAYC_metadata_test(1).csv" \
    --run-dir runs/final --output prediction.csv                          # 约 16 秒
python scripts/validate_submission.py --prediction prediction.csv --run-dir runs/final
```

产物清单与复现核验记录见《代码与复现说明》第 3、6 节。
