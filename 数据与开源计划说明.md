# 上下文阶梯 ContextLadder —— 数据与开源计划说明

## 1. 官方数据使用边界

本项目使用的官方竞赛数据仅用于本竞赛允许的开发、调试、模型训练、评估和提交。官方非公开原始数据不随公开代码包重新分发。代码包通过配置文件指定数据位置（`configs/data_paths.yaml` 指向包内 `input/` 目录），评审方在获得官方授权数据后放入该目录即可复现。

训练数据划分严格遵守官方「数据使用约束与最终评测」条款：最终提交模型只使用 `split_final == "train"` 的 5,920 行；验证划分的 3,038 行仅用于模型选择时的指标计算，不参与训练，也不用于估计任何统计量（含保留蛋白列表与归一化参数）。该边界可用 `python scripts/validate_submission.py --prediction prediction.csv --run-dir runs/final` 独立复核。

## 2. 最终候选模型的外部资源边界

最终候选模型使用官方提供的样本元数据和蛋白标签，不使用外部菌株蛋白组矩阵，不使用未经确认的 `strain_id → accession` 映射，也不使用外部基因组特征作为最终预测输入。

项目过程中曾进行公开化学资源、RDKit 和公共菌株/基因组资料的探索性检索，也曾从 1011 Yeast Genomes 论文的公开 supplementary table 取得候选的 `strain_id → 公共菌株名` 对齐用于探索性实验。这些资源**全部没有进入最终候选模型的预测输入**。完整检索记录、来源链接与不确定性判断见本代码包内的两份报告：

- `reports/DATA_PROVENANCE_SEARCH_20260811.md`——外部公开资源检索记录与来源链接；
- `reports/ACCESSION_MAPPING_AUDIT.md`——`strain_id → accession` 映射的可追溯性审计（含候选对齐及其未被采用的理由）。

两份报告均为按日期保留的历史内部记录，文首编者按说明了它们与本次提交的关系；其中引用的 `outputs/` 路径属于团队开发仓，不在本代码包内。

### 2.1 分析侧使用的外部公开资源（不进入预测模型）

本代码包内的 `external_data/processed/` 随包分发了**分析与证伪工作**所使用的派生外部特征，它们服务于《方案说明文档》第七节的表征通道实验，**不进入最终候选模型的任何预测路径**：

- 化合物机理文本嵌入 φ_TXT（`compound_text_embeddings/`）——由 `NeuML/pubmedbert-base-embeddings` 在锁定 revision 上生成，mean pooling，无 L2 归一化；
- 结构化机理先验 φ_SSPS（`ssps_priors/`）与化合物映射特征（`compound_mapping_features.csv`）。

来源、版本、许可、覆盖率与弃权规则（无实质机理信息的化合物一律给零幅度，不猜测）逐条列于 `external_data/RAW_SOURCES.md`。这些资源在第七节全部得到**负结果**，是证伪链条的一部分，随包分发是为了让该结论可被独立复核。

对应官方要求「须列出全部外部数据来源」：**最终候选模型的外部数据来源清单为空**——预测输入只有官方发放的样本元数据字段（菌株、化合物、培养基、温度、时间、数据来源、仪器、板号），无任何外部数据库、序列、结构或化学描述符进入模型。

公开资料中与虚拟酵母研究方向相近的项目只能作为背景线索，不能被表述为当前竞赛数据的样本级来源证明。

## 3. 依赖与许可证

代码使用的主要开源依赖及版本记录在：

- `requirements_submission.txt`（精简 pip 锁定版本，可直接安装）；
- `requirements_tl_snapshot.txt`（conda 完整快照，仅供环境对照）；
- `configs/env-spec.yaml`。

本代码包的自研代码、配置与文档采用 **CC BY-NC 4.0（署名-非商业性使用 4.0 国际）** 许可，许可文本见代码包根目录 `LICENSE`；第三方资源许可与边界见 `LICENSES/THIRD_PARTY.md`。若官方规定与项目许可证存在冲突，以官方要求和原始项目许可证为准。

## 4. 开源计划

复赛阶段提交训练/推理源代码、冻结配置、三个成员 checkpoint、环境说明、测试、许可与技术文档。对于官方非公开数据，不公开数据本身；公开可复现的内容包括：

- 模型结构；
- 数据读取和字段对齐逻辑；
- 训练和推理入口；
- 随机种子和超参数；
- 输出格式校验；
- 实验结果和局限性说明。

完整复现包已随本次提交给出（四条主命令、权重、清单四件套与免数据测试），以逐文件 SHA-256 冻结，不依赖任何外部仓库的可达性。**赛后将开源至公开仓库**，许可 CC BY-NC 4.0，范围即上列内容——官方非公开数据本身不在其中，也不会以任何派生形式发布。仓库地址在赛后确定并按官方要求提供；若后续阶段另需容器镜像，同样按官方要求补充，并进一步明确外部资源、版本和许可证。

## 5. 参赛项目边界声明

- 作品名称：**上下文阶梯**（ContextLadder）；技术代号 HCCE-Proteome。
- 团队名称：TorchDragon。
- 李晓蒙：方法设计与算法实现；
- 徐逸飞：生物领域专家，负责建模与科学问题界定。
- 本方案基于本团队自己的代码与实验记录整理；未把未由团队实际完成的外部工作写成团队原创贡献。

