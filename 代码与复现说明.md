# 上下文阶梯 ContextLadder —— 代码与复现说明

## 1. 代码组成

本代码包包含（除 `prediction.csv`、`LICENSE` 与四份说明文档外）：

```text
# —— 提交路径（生成 prediction.csv 所需的全部代码）——
scripts/build_embeddings.py                      # 命令 1：外部特征构建（本方案无需执行）
scripts/train.py                                 # 命令 2：从头训练最终模型，落盘 checkpoint
scripts/predict.py                               # 命令 3：冻结模型推理，生成 prediction.csv
scripts/validate_submission.py                   # 命令 4：提交格式与数据边界校验
scripts/a2b_train_variants.py                    # A2 掩码训练变体与训练流程
scripts/run_hcce.py                              # HCCE-Proteome 模型、train-only 蛋白筛选、训练流程
src/
configs/final.yaml                               # 冻结的最终配置（结构/超参/种子/集成/边界）
configs/data_paths.yaml
configs/field_mapping.yaml
configs/protein_feature_contract.json            # 5,243 原始列契约（历史口径参照）
configs/env-spec.yaml

# —— 历史入口（初赛口径，产出 5,243 列，已弃用，仅为溯源保留）——
scripts/make_mask_compound_3seed_submission.py   # 初赛最终入口（训练+推理合体）
scripts/make_hcce_submission.py                  # 历史 HCCE 50/50 单种子入口
scripts/make_submission.py                       # 历史 FiLM 单模型入口
scripts/run_p5_interactions.py
scripts/run_baselines.py
scripts/p0_audit_data.py

# —— 分析与证伪侧（对应《方案说明文档》的实验结论，不进入预测路径）——
scripts/evaluate_official_modules.py             # 官方口径六模块评估器
scripts/compound_invariance_check.py             # 化合物不变性恒等式检查
scripts/gate_w_summary.py                        # Gate-W 加权分与 ΔW 汇总
scripts/ensemble_eval.py                         # 多 seed 集成与验证集预测导出
scripts/dcb_build_cache.py                       # 嵌套 LOCO 共享缓存构建
scripts/dcb_loco_harness.py                      # 37 折嵌套无泄漏 LOCO harness
scripts/build_entity_mapping.py                  # 生成 external_data/entity_mapping.csv
scripts/build_source_manifest.py                 # 生成 external_data/source_manifest.json
scripts/build_reproducibility_manifest.py        # 生成 REPRODUCIBILITY_MANIFEST.json
configs/dcb_gates.json                           # 冻结判据（DCB-40）
configs/p05_gates.json / p1_gates.json / p3_gates.json      # 各阶段冻结判据
configs/m1_candidates.json / p05_candidates.json / p3_candidates.json   # 冻结候选清单
external_data/RAW_SOURCES.md                     # 外部资源来源/版本/覆盖率/弃权规则
external_data/entity_mapping.csv                 # 实体映射（含未匹配/歧义/代理，全部 used_by_final_model=False）
external_data/source_manifest.json               # 机器可读的外部来源/版本/许可/校验和
external_data/processed/                         # 分析侧派生外部特征（不进入预测模型）

# —— 训练产物（按命令 2 跑出来的结构）——
runs/final/checkpoints/seed{20260810,3407,42}.pt # 三个集成成员权重，各约 28.6MB
runs/final/checkpoints/run_manifest.json         # 配置/输入/成员 checkpoint 的 SHA-256
runs/final/artifacts/preprocess_contract.json    # 4,422 蛋白契约、过滤规则、拟合行数
runs/final/artifacts/protein_list.txt            # 4,422 列名与顺序
runs/final/artifacts/train_protein_mean_kept.npy # train-only 逐蛋白均值
runs/final/artifacts/embeddings/embedding_manifest.json

# —— 清单与校验 ——
REPRODUCIBILITY_MANIFEST.json                    # 总清单：命令/配置/权重/预测/边界/已知限制
prediction_manifest.json                         # prediction SHA-256 与 prediction_scale
validation_report.json                           # 提交格式校验结果
tests/                                           # 免数据契约测试（pytest）
LICENSES/                                        # 项目许可 + 第三方资源许可

# —— 依赖与记录 ——
requirements.txt
requirements_submission.txt
requirements_tl_snapshot.txt
reports/                                         # 外部资源披露 + 各阶段实验报告（见下）
```

`reports/` 内含：`DATA_PROVENANCE_SEARCH_20260811.md`、`ACCESSION_MAPPING_AUDIT.md`（外部资源披露），
以及 `P1_BASELINE_RESULTS.md`、`M0_MODULE_EVAL_RESULTS_20260816.md`、`DCB40_EXECUTION_RESULTS_20260816.md`、
`P05_SCENARIO_BASELINES_20260826.md`、`P1_OPTIMIZATION_LEVERS_20260826.md`、
`P15_CONTROL_POOL_POSITION_20260826.md`、`P2_DENSE_PRIOR_20260826.md`、
`P3_CAPACITY_EMA_BLEND_20260827.md`（各阶段冻结判据下的执行报告，含全部负结果）。

**候选清单说明**：`configs/*_candidates.json` 中的 parquet 路径指向团队开发仓的 `outputs/`，
不随本代码包分发。它们随包提供是为了让"哪些候选在哪条冻结判据下被评过"可核对，
不是可直接运行的输入。

其中：

- `train.py` / `predict.py` 是最终提交路径：前者只读训练 metadata 与 proteome、落盘三个成员 checkpoint 与预处理契约，后者只读测试 metadata 与 run 目录、生成 `prediction.csv`。**两者分离，推理脚本不含任何蛋白组读取路径**；
- `build_embeddings.py` 是规范要求的第一条命令。本方案不使用任何外部特征，该步骤**无需执行**，运行它只会写出一份声明此事的 manifest；
- `validate_submission.py` 校验提交格式（4,454 行 × 4,422 列、log2、有限值、顺序）与数据边界，无需重跑训练；
- `make_mask_compound_3seed_submission.py` 是 2026-08-15 初赛最终入口（训练+推理合体，产出 5,243 列），**口径已弃用**，仅为溯源保留；
- `a2b_train_variants.py` 定义 A2 掩码实体训练变体与训练流程；
- `run_hcce.py` 定义 HCCE-Proteome 基础模型、train-only 蛋白筛选与训练流程；
- `make_hcce_submission.py` 与 `make_submission.py` 是更早的历史提交入口，同为 5,243 列口径，**均已弃用**；`run_p5_interactions.py` 是对应的 FiLM 训练/交互实验脚本，只在训练划分上拟合；
- `run_baselines.py` 是均值/Ridge 基线；
- `p0_audit_data.py` 是数据审计脚本；
- `evaluate_official_modules.py` / `compound_invariance_check.py` / `gate_w_summary.py` / `ensemble_eval.py` / `dcb_*.py` 是分析与证伪侧脚本，服务于《方案说明文档》的实验结论，**不在最终预测路径上**；
- `reports/` 保存外部资源检索与 accession 映射审计记录，对应《数据与开源计划说明》第 2 节的披露要求；
- `src/` 包含数据读取、对齐、`log2` 转换、评测与对照匹配等基础逻辑；
- `configs/final.yaml` 冻结最终模型的结构、超参、随机种子、集成成员与权重、蛋白过滤规则、输出格式与数据边界；其余 `configs/` 保存数据路径、字段映射、历史列契约和环境信息；
- `runs/final/` 是按命令 2 跑出来的训练产物目录；`REPRODUCIBILITY_MANIFEST.json` 把配置、入口、权重、预测串成一条可核对的版本链。

## 2. 环境

- Python：3.8.18；
- PyTorch：2.1.0；
- NumPy：1.24.3；
- pandas：2.0.3；
- scikit-learn：1.2.2；
- PyArrow：17.0.0，分析侧脚本读写 parquet 需要（`src/mosaic/contracts.py` 在模块层导入）。**只跑最终提交入口不需要它**；
- SciPy：1.10.1；
- RDKit：仅 `dcb_loco_harness.py` 的 Tanimoto 对照通道用到，且为函数内延迟导入；最终候选模型不依赖其特征，不安装不影响提交路径；
- 推荐设备：CUDA GPU；CPU 也可以执行，但速度较慢。

依赖快照见 `requirements_submission.txt`（精简 pip 锁定版本，可直接安装）与 `requirements_tl_snapshot.txt`（conda 完整快照，仅供环境对照）；环境摘要见 `configs/env-spec.yaml`。

## 3. 运行步骤

四条主命令。官方数据按 `input/README.md` 放入 `input/` 后，可直接复制执行。

```bash
cd <代码包根目录>
conda activate tl   # 或 pip install -r requirements.txt

# 1) 构建外部特征 —— 本方案不使用任何外部特征，无需执行
python scripts/build_embeddings.py --output artifacts/embeddings

# 2) 从头训练最终模型（三个种子，单张 RTX 3090 约 100 秒）
python scripts/train.py \
  --metadata "input/WAYB_WAYC_metadata_train_val(1).csv" \
  --proteome input/WAYB_WAYC_proteome_raw_train_val.csv \
  --config configs/final.yaml --output-dir runs/final

# 3) 冻结模型推理，生成 prediction.csv（约 16 秒）
python scripts/predict.py \
  --metadata "input/WAYB_WAYC_metadata_test(1).csv" \
  --run-dir runs/final --output prediction.csv

# 4) 校验提交格式与数据边界（无需重跑训练）
python scripts/validate_submission.py --prediction prediction.csv --run-dir runs/final
```

不带 `--metadata` / `--proteome` 时，脚本按 `configs/data_paths.yaml` 取默认路径，效果相同。

**断点续训与失败恢复**：不提供断点续训。单成员训练约 30~40 s、三个成员全程约 100 s，
中断后直接重跑命令 2 即可；`train.py` 每次运行完整重写 `runs/final/` 下的 checkpoint 与
artifacts，不读取也不依赖任何中间状态，重跑无残留风险。

命令 2 生成：

```text
runs/final/checkpoints/seed20260810.pt          # 集成成员权重（约 28.6MB）
runs/final/checkpoints/seed3407.pt
runs/final/checkpoints/seed42.pt
runs/final/checkpoints/run_manifest.json        # 配置/输入/权重的 SHA-256 与环境版本
runs/final/artifacts/preprocess_contract.json   # 4,422 蛋白契约、过滤规则、fit_rows
runs/final/artifacts/protein_list.txt           # 4,422 列名与顺序
runs/final/artifacts/train_protein_mean_kept.npy
```

命令 3 生成 `prediction.csv` 与 `prediction_manifest.json`（含 `prediction_scale: log2` 与 SHA-256）；
命令 4 生成 `validation_report.json`。

**关于 checkpoint 文件哈希**：`seed*.pt` 内除权重外还存了 `config_hash`，以便把权重与配置绑定。
因此**修改 `configs/final.yaml`（哪怕只是注释）会改变 checkpoint 的文件哈希，但不改变权重本身**——
两版权重的 56 个张量逐元素 `max|Δ| = 0`，生成的 `prediction.csv` SHA-256 也不变。

**关于加载 checkpoint**：文件里含一个 pickled 的 `HCCEMetaEncoder` 对象，其类定义位于 `run_hcce`。
`predict.py` 会先调用 `load_run_hcce()` 注册该模块再 `torch.load`；若要在自己的脚本里直接加载，
需先执行 `from scripts.a2b_train_variants import load_run_hcce; load_run_hcce()`。

**三个成员 checkpoint 随包分发**，SHA-256 记录在 `runs/final/checkpoints/run_manifest.json` 与
`REPRODUCIBILITY_MANIFEST.json`；`predict.py` 在加载前逐个校验哈希，不一致直接中止。

## 4. 随机性和数据边界

- 随机种子：`20260810`、`3407`、`42`（三 seed 等权集成）；训练轮数：40；embedding 维度：64；化合物掩码率：0.25。
- DataLoader shuffle 使用固定种子。
- GPU 随机种子、cuDNN deterministic 和 benchmark 设置已固定。
- **不使用早停**：训练轮数固定为 40，由冻结验证集在模型选择阶段一次性选定后写入 `configs/final.yaml`；训练过程中不做 checkpoint 挑选，因此复现运行不会因停在不同 epoch 而分歧。
- 优化器 AdamW，lr 1e-3，weight decay 1e-4；损失为观测掩码下的 MSE（缺失蛋白不产生梯度），在 concat / FiLM / legacy 三个头上平均，另加模型自身的正则项。
- **实测资源（单张 RTX 3090）**：训练 97.6~107.7 s、推理 16.2~18.9 s、峰值显存 **923 MiB**。
- 干净目录复现的完整控制台输出与逐秒显存采样随包提供于 `logs/`。

数据边界严格对应官方「数据使用约束与最终评测」条款：

| 项目 | 拟合范围 | 代码位置 |
|---|---|---|
| 训练样本 | `split_final == "train"`（5,920 行） | `train.py` 的 `fit_indices`，并带硬断言：任何非 train 行进入即中止 |
| 蛋白保留列表（缺失率 < 0.8） | 同上 | `run_hcce.py: apply_official_protein_filter` |
| 六个类别字段映射表 | 同上 | `a2b_train_variants.py: HCCEMetaEncoder.fit` |
| 温度/时间标准化与 RBF 中心 | 同上 | 同上 |
| 目标 `mean` / `std` | 同上 | `a2b_train_variants.py: fit_model_variant` |
| 4,422 蛋白列表与顺序 | 同上 | `train.py` 写出 `runs/final/artifacts/protein_list.txt`，SHA-256 入 manifest |

- 验证划分（`val_strain_only` / `val_chem_only` / `val_both` / `val_time`，合计 3,038 行）只用于模型选择阶段的指标计算，不参与训练，也不用于估计上表任何统计量。
- 测试蛋白组真值不在最终提交脚本中加载。**`predict.py` 不含任何蛋白组读取路径**（`tests/test_submission_contract.py` 对此有断言），`train.py` 也不打开测试元数据；测试元数据仅在推理时读取。
- 方案说明文档第七节的测试自评指标，在预测文件冻结并通过契约校验之后才计算，未回流到任何选择决策。
- **静态合规审查（验收流程第 2 步）**：包内出现测试真值路径的位置一共 14 处，逐条列在包根的 `TEST_TRUTH_ACCESS.md`——其中真正读取该文件的只有 `scripts/p0_audit_data.py` 一个独立审计工具，它不在四条主命令的链路上，不放测试真值文件时四条命令依然全部成功（干净目录实测）。其余各处分别是配置里的路径声明、`src/mosaic/contracts.py` 的禁读清单、`tests/` 的静态断言，以及文档中的边界说明。我们把这份清单直接写出来，而不是让审查方逐个判断。

## 5. 输出契约

脚本自动检查：

- 行数等于官方测试样本数（4,454）；
- 样本 ID 唯一；
- 样本 ID 顺序与测试元数据一致；
- 蛋白列名与顺序与 `runs/final/artifacts/protein_list.txt` 完全一致（**4,422 列**，即复赛提交说明规定的标准建模空间）；
- 所有预测值为有限值；
- 输出空间为 `log2(raw)`，不是 z-score，也不是 raw intensity。

检查结果写入 `validation_report.json`（当前结果：`verdict: PASS`，九项硬检查全过，
`fit_rows: 5920`，`held_out_val_rows: 3038`）。

评审方可不重跑训练、直接独立复核：

```bash
python scripts/validate_submission.py --prediction prediction.csv --run-dir runs/final
```

该脚本重新读取 `prediction.csv`、官方测试元数据与 `runs/final/artifacts/protein_list.txt`，
独立重算行数、列数、ID 唯一性与顺序、蛋白列顺序、有限值、log2 尺度，并回读
`preprocess_contract.json` 复核训练边界。

> 关于 4,422 与 5,243：初赛口径要求覆盖官方 5,243 列契约，模型预测其中 4,422 列、其余 821 列
> 以 train 划分均值填充。**复赛提交说明改为要求 `sample_ID` + 4,422 列**，因此填充步骤不再出现在
> 提交产物中。复赛说明同时写明标准建模空间为 4,422 个蛋白，按其规则实算恰好得到 4,422，口径一致；
> 未下发的是 contract **文件本身**，故列顺序取自官方 `train_val` 矩阵的原始列序。列表与 SHA-256
> 落盘于 `runs/final/artifacts/protein_list.txt`，相关说明见 `REPRODUCIBILITY_MANIFEST.json`。

## 6. 复现核验记录

> **口径更新（2026-08-29）。** 下方 6.1 记录的是 **2026-08-27 对初赛口径管线**（合体入口、
> 5,243 列产物）的干净目录实测。那次核验真实发生且全部通过，但它验证的管线已被本次改造取代：
> 提交入口拆分为 `train.py` / `predict.py`，提交产物改为 4,422 列。**6.1 的数值不再描述随包文件**，
> 保留是为了记录改造前的可复现性事实。当前口径的核验记录见 6.2。

### 6.1 初赛口径管线（2026-08-27，已被取代）

为验证本代码包在评审方手中确实可复现，我们把代码包**复制到一个与开发仓无关的干净目录**，
按 `input/README.md` 放入官方数据后，以第 3 节的命令原样执行了一次完整重训与推理：

| 核验项 | 结果 |
|---|---|
| 入口 `scripts/make_mask_compound_3seed_submission.py` 退出码 | 0 |
| 口径 | 初赛：合体入口，`sample_ID` + 5,243 列 |
| 重训 + 推理耗时（单张 RTX 3090） | 121.7 s / 124.6 s（两次独立执行） |
| 生成的 `prediction.csv` SHA-256 | `a7cff2acd719bd6d7bb09e307eae96a4a81b037e16a667c2d114d113aaaf4d43` |
| 与随包 `prediction.csv` 比对 | **逐字节相同**（同一 SHA-256，228,979,792 字节） |
| `SUBMISSION_VALIDATION.json` 契约 | `submission_contract: PASS`，`fit_rows: 5920`，`held_out_val_rows: 3038` |
| `scripts/verify_submission.py`（已随本次改造删除） | **13 项全部 PASS**（行数/ID 唯一性/ID 顺序/列名与顺序/有限值/log2 空间/无 0.0 占位/train-only 拟合/验证集留出/入口不引用 test 蛋白组） |
| 分析侧五个脚本从包根 `--help` | 全部导入成功 |
| 环境版本与 `configs/env-spec.yaml` | 逐条一致（Python 3.8.18 / torch 2.1.0 / numpy 1.24.3 / pandas 2.0.3 / sklearn 1.2.2） |

两次执行之间同步了 `scripts/a2b_train_variants.py`（新增 `--ema-decay` 参数，默认 `0.0` 即禁用）
与新增的分析侧脚本；**第二次执行的输出与第一次、与随包文件仍然逐字节相同**，
证明该参数的加入不改变提交路径的任何行为。

复现所依赖的确定性设置在 `run_hcce.py: seed_everything` 中固定：
`random.seed` / `np.random.seed` / `torch.manual_seed` / `torch.cuda.manual_seed_all`，
以及 `torch.backends.cudnn.deterministic = True`、`torch.backends.cudnn.benchmark = False`；
DataLoader 的 shuffle 使用 `torch.Generator().manual_seed(seed)`。

### 6.2 当前口径管线（2026-08-29，干净目录实测）

把代码包复制到与开发仓无关的目录，排除 `runs/`、`prediction.csv` 与各 manifest，
按 `input/README.md` 放入官方数据（**只放 train_val 的 metadata/proteome 与 test 的 metadata；
`WAYB_WAYC_proteome_raw_test.csv` 全程未放入**），按第 3 节四条命令原样执行：

| 核验项 | 结果 |
|---|---|
| 口径 | 复赛：`train.py` / `predict.py` 分离，`sample_ID` + **4,422** 列 |
| 命令 1 `build_embeddings.py` | exit 0（如实报告"本方案无需执行"） |
| 命令 2 `train.py`（三个种子） | exit 0，**97.97 s**（单张 RTX 3090） |
| 命令 3 `predict.py` | exit 0，**17.45 s** |
| 命令 4 `validate_submission.py` | **verdict: PASS**，九项硬检查全过 |
| 峰值显存（GPU 0） | **923 MiB** |
| **三个 checkpoint SHA-256** | 与随包 `run_manifest.json` **完全一致** |
| **`prediction.csv` SHA-256** | `59f99dc4…2b17e`，**与随包文件同一值** |
| 包根 `pytest tests/` | **12 passed** |
| 分析侧五个脚本从包根 `--help` | 全部导入成功 |
| 环境版本 vs `configs/env-spec.yaml` | 逐条一致 |
| 全新 venv 按 `requirements_submission.txt` 安装 | **exit 0**（2026-09-01 首次实测，此前只做过版本比对） |
| 新 venv 内 `pytest tests/` | **12 passed** |

**测试蛋白组从未放入，四条命令仍全部成功。** 这是构造性证据而非声明：`predict.py` 中不存在
任何蛋白组读取路径，`tests/test_submission_contract.py` 对此有静态断言。

确定性另有两条旁证：开发机上两次独立训练的三个 checkpoint SHA-256 完全相同；改造前后在
4,422 个模型预测列上 `max|Δ| = 0.000000e+00`，说明入口拆分未改变模型。

完整记录见 `reports/P0_REPRODUCTION_HARDENING_20260901.md`（更早的 08-27 / 08-29 / 08-30 三版保留备查，均已标注被取代）。

复现所依赖的确定性设置在 `run_hcce.py: seed_everything` 中固定：
`random.seed` / `np.random.seed` / `torch.manual_seed` / `torch.cuda.manual_seed_all`，
以及 `torch.backends.cudnn.deterministic = True`、`torch.backends.cudnn.benchmark = False`；
DataLoader 的 shuffle 使用 `torch.Generator().manual_seed(seed)`。
**跨硬件的 GPU 归约不保证位确定**，上述一致性均在同型号 RTX 3090 上取得。

## 7. 分析侧结论的可复核范围（诚实边界）

`reports/` 中各阶段报告的结论由分析侧脚本产出。这些脚本随包分发且可导入运行，但
**它们的输入是团队开发仓 `outputs/` 下的候选预测 parquet，按数据边界不随本代码包分发**。
因此评审方可以独立复核的是：

- **可以**：最终提交路径的完整重训与推理（第 6.2 节，逐字节可复现）；
- **可以**：提交格式与训练边界（`scripts/validate_submission.py`，无需重跑训练）；
- **可以**：审读评估器、不变性检查、LOCO harness 的**实现**，以及各阶段**冻结判据**
  （`configs/*_gates.json`，均记录了冻结时点与 SHA-256）；
- **需要重跑候选才能复核**：`reports/` 中的模块分与 ΔW 数值——评审方若要完整复核，
  需先用本包入口重建对应候选的验证集预测，再按各报告"可复现性"一节给出的命令执行。

我们不宣称这些数值在不重建候选的前提下可被本包单独复核。
