# 上下文阶梯 ContextLadder —— 方案说明文档

## 一、作品名称

**上下文阶梯：面向未见扰动与未见菌株的虚拟酵母蛋白组预测**
*ContextLadder — Hierarchical Context Fallback for Unseen Perturbations and Strains*

技术代号：**HCCE-Proteome**（层级上下文条件化蛋白组模型，A2 化合物掩码训练 + 三种子集成）。

「阶梯」指本方案的核心机制：模型对实验条件的依赖被组织成一道可以逐级回退的阶梯。测量上下文中，板（plate）建模为仪器与来源之上的收缩残差，遇到未见板时自动退回仪器/来源这一级；化学扰动上，25% 的化合物 token 在训练时被掩码为 `<UNK>`，迫使模型在遇到未见化合物时退回生物与测量上下文的先验，而不是凭一个陌生 ID 硬编出扰动信号。两条分支是同一个原则的两次实例化——**宁可退到更粗的一级，也不要在没有证据的地方假装知道**。

复赛阶段我们补上了这条原则的**依据**：第四至第七节的四项测量表明，在未见化合物上，"退回"目前不是保守选择，而是唯一有证据支持的选择。

## 二、团队信息

- 团队名称：TorchDragon
- 李晓蒙：方法设计与算法实现
- 徐逸飞：生物领域专家，负责建模与科学问题界定

## 三、研究问题与本方案的出发点

本项目面向虚拟细胞算法任务，目标是在给定实验上下文和化学扰动条件下，预测酵母细胞的多蛋白表达响应。模型输出覆盖官方蛋白目标集合，并严格遵循官方规定的 `log2(raw)` 输出空间。输入包括化学扰动身份、菌株/培养基/数据来源/仪器/板等实验上下文，以及温度和扰动时间等连续条件；输出为与官方蛋白特征契约一致的多蛋白响应向量。

初赛阶段我们把工作放在"把这个回归做好"。复赛阶段我们先退一步问了一个不同的问题：**这个任务的分数，具体在测什么？** 第四至第七节是这个问题的四个答案，第八节说明它们如何反过来决定了方法设计。

## 四、发现一：占 20% 权重的模块上，没有模型贡献化合物信息

官方评分中 **20% 是"上下文均值残差"**——用同一批次内训练药物的平均响应作参照，考察对**未见化合物特异响应**的预测能力。这是整份赛题里最直接对应"零样本扰动泛化"的一项。

我们对它做了一次机械检查：在冻结验证集上，取每个上下文块内**携带不同未见化合物**的行对，比较它们的预测蛋白组向量。

- **S1（已见菌株 + 未见化合物）：379 个含多化合物的上下文，991 个跨化合物行对，991 对逐位相同，最大偏差精确为 0。**
- **S3（双重未知）：96 个上下文，250 对，250 对逐位相同，最大偏差精确为 0。**

（`outputs/p05_scenario_baselines/compound_invariance.json`）

这不是本方案的特例。同一检查跑在**五个候选**上——本方案、两个全局梯度提升基线、一个分场景低秩统计基线、一个分场景梯度提升基线——**结果完全一致**。更早一轮在八个不同架构（FiLM / HCCE / CA-HCCE / 逐蛋白 EB 校准等）上得到相同结论（`reports/M0_MODULE_EVAL_RESULTS_20260816.md`）。

一项**独立的、更早的**测量指向同一结论：若剥离 Δ_pred 与 Δ_true 共享的 −y_control 项、改用模型内部差分（ŷ_treat − ŷ_同批对照）度量纯粹的化合物特异效应，本模型在已见化合物上为 **0.175**，在未见化合物上仅 **0.042**。

**结论：在这个任务上，目前所有可得的模型对未见化合物只发出一个向量——每个上下文一个，与是哪一种化合物无关。** 20% 权重的这一项，得分完全来自所有化合物共享的分量（对照锚定与上下文均值）。

### 4.1 这个发现的意义

模型在 S1 上拿到的 0.203387 不是"对新药机制的预测"，而是"对该批次共享响应与对照锚定的预测质量"，只是通过残差模块的算式表现出来。

这有一个可直接验证的推论，本轮验证了它：**如果 S1 分数只反映共享分量的预测质量，那么不同模型的 S1 差距应当等同于它们绝对预测质量的差距，而与"化合物建模能力"无关。** 实测符合——五个候选的化合物信息同为零，而 S1 分数从 0.073488 到 0.203387 相差近三倍，这个差距完全由绝对保真度与对照锚定质量决定（本方案 abs-PCC 0.988598 / RMSE 0.420259；四个经典基线中最好的 abs-PCC 为 0.981191、最低 RMSE 为 0.549638，分别来自两个不同候选）。

**对社区的判断是：S1 这个模块目前测的是"共享上下文响应 + 对照锚定"的预测质量，不是"新化合物特异响应"的能力。** 在有模型能通过第七节给出的表征门槛之前，该模块的排名不应被解读为零样本机制泛化能力的排名。

### 4.2 三条必要的限定

1. **这不是"我们的模型不行"。** 它是当前所有被测模型的共同性质，包括官方基线家族。在化合物信息确实可用的场景上，本方案是平凡基线的数倍（第九节）。
2. **这不是一个统计检验的结论。** 我们最初用化合物标签置换检验来测它，随后发现该检验在此处**零功效**：`null_sd = 0.0`，p = 1.0 是恒等式而非结果——所有未见化合物本就映射到同一个 unknown token，置换不改变任何输入。我们撤回了该表述，改按其本来面目报告：**这是不变性恒等式，不是检验**（`reports/P05_SCENARIO_BASELINES_20260826.md`）。
3. **它本身不说明"化合物信息不可能获得"。** 那需要另外的证据，见第七节。

## 五、发现二：选择目标不等于评分目标

官方 25% 的 FC 模块要求把处理样本匹配到**同来源、菌株、培养基、温度、时间、仪器、板号**下的 DMSO 或 Water 对照。对照池从哪里取，赛题文本未作限定，而这个选择会**静默改变被评估的样本集合**。

以官方口径（对照池 = `train | val`）计算，验证集共 **2,806** 个匹配行，分层为 seen 139 / S1 1,065 / S2 1,333 / S3 269。若改用**仅训练池**的对照，**S2 的 1,333 行与 S3 的 269 行会全部失去匹配对照而被丢弃——共 1,602 行，占 57.1%**。

被丢掉的恰好是两个最难、得分最低的场景。**任何在训练池对照下做的模型选择，都对 57% 的匹配行是盲的。** 我们自己在 2026-08-13 至 08-15 的多轮 gate 就踩在这个坑里，是在 08-16 重建官方口径评估器时才发现的。

配套的两个发现：

- **只看单一 RMSE 会错排相关性模块。** 八个候选中，CA-HCCE 的 FC 全体分数最高（0.491982），但 S1 排名倒数第三（0.166947）；两个相关性模块的排序与 RMSE 排序不一致。
- **μ_ctx 的上下文键此前被我们当作赛题未定义的量，实际上赛题定义了它**："剔除同一**菌株/培养基/温度/时间/冻结批次**下的共享药物响应分量"——不含来源、不含仪器（与同一份文档中 FC 对照匹配键明确不同）。把唯一未绑定到列的"冻结批次"分别绑到板号与来源两种读法后实测，**两者与完整严格键给出逐位相同的结果**（S1 = 0.203387，n = 1,059，八个候选全同）。因此 S1 的官方读数唯一确定；此前作为敏感性上界报告的生物学粒度数值（0.5512）**不是官方读法**，其表面增益是伪信号，我们不再引用。

这三件事对任何参赛队都适用，我们把评估器开源（第十二节），使其可被独立复核。

## 六、发现三：这个划分能验证什么、不能验证什么

**S1 侧**：验证集只有 **6 个未见化合物**，即 6 个聚类。聚类 bootstrap 标准差 0.0265，95% 区间 [0.139, 0.235]。任何小于约 **0.053** 的 S1 改动在这个划分上**不可判定**。我们把 0.053 冻结为效应量地板，此后没有为任何候选下调过它。

代价是真实的：R1 凸混合是测过的八个候选中**唯一**改善 S1 的（w = 0.10 时 +0.02191，配对 bootstrap CI [+0.00797, +0.03853]，两种粒度同号，其余模块不退化），仍因 +0.0219 < 0.053 被判 FAIL，未进入提交线。

**S2 侧的问题更严重**：验证集只提供**一个**未见菌株（BAI），即单聚类；而测试集 **59.8%（2,663/4,454）**的行落在未见菌株上，主力未见菌株 **CRD 有 2,231 行，在 `train_val` 中一行都没有**。因此**在 BAI 上选出的任何菌株侧改动，对 CRD 没有任何泛化证据**——不是"证据弱"，是"选择实体与评测实体不同"。我们据此关闭了整条菌株侧建模分支，并在所有加权判据中强制并列报告去掉 S2 项的版本。

**这是 benchmark 设计层面的观察，不是抱怨。** 可操作含义是：该划分能支持关于绝对保真度、FC 与时间插值的结论；对于"未见菌株背景调制"这一 20% 权重的模块，它目前只能支持描述性报告，不能支持模型选择。

## 七、发现四：缺的不是模型容量，是化合物表征

既然第四节说明当前模型在未见化合物上零信息，自然的问题是：**这是模型不够强，还是信息本来就拿不到？**

我们在 37 个训练化合物的**完全嵌套、无泄漏**留一交叉验证上测了这件事（`reports/DCB40_EXECUTION_RESULTS_20260816.md`；harness 用稀疏解码器逐位复现了独立实现的参考值 0.005872 / −0.000727 / 0.4636，37 折泄漏断言全过）。判据在任何真实数据进入管线**之前**冻结（`configs/dcb_gates.json`），此后未移动。

**容量够。** 训练化合物残差的线性张成（全张成 k = 36）中存在与目标对齐 **ρ_oracle = 0.2671** 的方向，聚类 bootstrap 单侧 95% 上界 0.2941（严格粒度）/ 0.2706（生物粒度），**均高于 0.20 的判定门槛**。

**解码器构造不是瓶颈。** "稠密低秩解码器 − 稀疏支撑解码器"的差在**四个通道上全部为负**（−0.005 ~ −0.019）。

**缺的是内容。** 所有可构造的化合物表征通道都打不过负对照：

| 通道 | ρ̄ | 95% CI |
|---|---:|---|
| φ_TXT（机理文本嵌入，预注册主通道） | 0.0268 | [−0.0117, +0.0664] |
| φ_SSPS（结构化机理先验，消融） | −0.0325 | [−0.0727, +0.0126] |
| Tanimoto 化学相似度（构造对照） | −0.0148 | [−0.0589, +0.0392] |
| 随机嵌入（负对照） | −0.0250 | [−0.0643, +0.0183] |

主通道经 maxT 多重性校正后同样不显著（观测 |均值| 0.0346 < 临界 0.0638）。

**稀疏先验这一族在数学上已经关闭。** 覆盖中位 58 个蛋白的稀疏机制先验，**即使方向全部正确**，全向量相关的结构上限也只有 **0.0491**；其中约一半（0.027）来自"稀疏且随便哪里符号对"本身，与机制知识无关（`reports/M0_MODULE_EVAL_RESULTS_20260816.md`）。同族实测先验的 Gate-0 得分为 ρ̄ = 0.0059，CI 含 0。

**四条合起来给出一条可被接续的定量结论：**

> 要让 20% 的 S1 模块产生真实的化合物特异信号，需要**覆盖 4,422 个蛋白中相当大比例、且方向优于随机的稠密先验**。换编码器无效（四个通道都试过），换解码器无效（稠密−稀疏全负），做更精细的稀疏机制先验在数学上不可能达标（上限 0.049 < 地板 0.053）。**oracle 0.267 与可实现 0.03 之间近十倍的差距，只能来自化合物表征本身。**

必须写明的限定：上述 ρ 在**留出训练化合物**上测得，而那是训练化学空间内的**插值**。实测 6 个验证集未见化合物中有 3 个落在训练集内部最近邻距离分布的 p90 之外，其中 Sulfometuron methyl 与 Pentamidine isethionate 位于 **100 分位**——比任何一对训练化合物之间都更远。**因此 0.0268 对真实的 S1 任务而言仍然偏乐观。**

## 八、核心方案：ContextLadder

第四与第七节合起来给出一个明确的设计约束：**对一个未见化合物，模型没有任何可用的化合物特异信息**（不是没做，是四个通道都试过、都打不过负对照）。在这个前提下，唯一有证据支持的行为是**退回到更粗的一级先验**，而不是让一个从未训练过的化合物 ID 去驱动输出。

### 8.1 测量分支的分层收缩：先有测量，后有设计

P1 基线阶段有一个意外结果（`reports/P1_BASELINE_RESULTS.md`）：**只用测量上下文**（来源/仪器/板号）的 Ridge 达到 abs-PCC **0.982272**，而只用生物上下文的 Ridge 只有 **0.962347**，两者合并的完整 Ridge 是 **0.984998**。测量批次几乎解释了全部可解释方差——典型的 batch shortcut 风险：模型容易退化成"板号查表器"，遇到未见板即崩溃。

设计上的回应不是丢掉板号（会损失大量真实信号），而是**把板号建模为来源/仪器之上的收缩残差**：见过的板享有自己的偏移，未见的板自动退回仪器/来源一级，且退回是结构性的，不需要任何运行时分支。

### 8.2 化合物掩码：把"不知道"从随机向量变成学出来的状态

未见化合物落到保留的 `<UNK>` 槽位，而该槽位的嵌入在训练中**从未收到梯度**——它是一个随机初始化的陌生向量，会驱动模型输出任意扰动信号。

我们先用一个不需要重训的探针定位该机制（A1）：把冻结检查点里的 `<UNK>` 嵌入替换为类别均值。结果**方向对但不稳健**——化合物侧 seed 20260810 的未见化合物子集 RMSE 从 0.378413 降到 0.366050（−0.0124），另两个 seed 只有 −0.0001 / −0.0018；**菌株侧三个 seed 全部退化**（整体 +0.0015~+0.0032，BAI 子集 +0.003~+0.006）。

结论：随机 `<UNK>` 确实在造成损害，但**事后打补丁不行，这个回退状态必须被训练出来**。于是 A2——训练时以 25% 概率把化合物 token 掩码为 `<UNK>`，标签不变。

**该设计有方向性、可被证伪，不是随手加的正则**，三条实测支撑：

| 变体 | 结果 | 说明 |
|---|---|---|
| 只掩码化合物（采纳） | 三 seed 全部优于不掩码基线；集成 RMSE **−0.003405** | 未见菌株子集 −0.003470、未见化合物子集 −0.001937，两个 OOD 子集同时改善 |
| **同时掩码化合物与菌株** | **RMSE +0.015（显著退化）** | 菌株特异性真实且可学，掩码它是破坏而非正则 |
| A1 + A2 组合 | 三 seed 全部退化（+0.0001~+0.0003） | 训练出的 `<UNK>` **严格支配**事后均值替换；A1 被取代而非互补 |

中间一行是关键：若这只是"加了个 dropout"，掩码菌株应当同样有益。它没有。**该设计针对一个具体的、被定位过的失效机制。**

### 8.3 双专家融合

分层结构对**已见实体**有代价——收缩削弱精确记忆。因此保留一个高容量 one-hot 专家（传统 FiLM），与分层专家做固定 50/50 融合。三 seed 验证：legacy 专家 sample-PCC 0.987670、HCCE 分层专家 0.987444、**50/50 融合 0.988140**——融合优于任一单专家，权重由仅使用训练数据的证据选定。

### 8.4 实现细节

1. **输入编码（unknown-safe）**：化合物、菌株、培养基、数据来源、仪器、板六个类别字段使用仅由训练数据构建的类别映射，未见类别统一落入保留的 `<UNK>` 槽位；温度与扰动时间经训练统计量标准化，时间维度额外使用 log-时间与 5 个 RBF 基构成的连续时间表示。
2. **HCCE FiLM 专家**：化学分支、生物分支（菌株+培养基+数值条件）与测量分支（来源+仪器+板）分路编码；测量分支采用 8.1 的层级化残差结构；生物分支通过 FiLM 调制化学表示，并保留 5% 权重的上下文直连通路，避免上下文信息被完全丢弃。
3. **传统 FiLM 专家**：高容量 one-hot 路线（化学 one-hot + 上下文 one-hot + FiLM 调制），作为对层级化路线的补充而非替代。
4. **A2 掩码实体训练**：25% 概率掩码化合物 token（仅化合物；双字段掩码为负结果，未采用）。
5. **融合与输出**：每 seed 最终预测 = 0.5 × 传统 FiLM + 0.5 × HCCE FiLM；三 seed 等权平均。多输出解码器一次预测全部保留蛋白目标。
6. **蛋白目标筛选**：仅用 `train` 划分计算缺失率，保留缺失率 < 80% 的 **4,422** 个蛋白——这正是复赛提交说明规定的标准建模空间，提交产物只含这 4,422 列。（初赛口径另需覆盖官方 5,243 列契约，其余 821 列以 `train` 逐蛋白 log2 均值填充，其中 189 个在 `train` 内无有效观测的蛋白回退为 `train` 全局均值 `21.5222` 而非 0.0——0.0 对应原始强度 1，远在观测值域之外。该填充步骤在复赛口径下不再进入提交产物。）
7. **提交重训**：固定结构、超参与轮数（40 轮、embedding 维度 64、掩码率 0.25），**仅在 `split_final == "train"` 的 5,920 行标签上**按三 seed 重训并集成。

### 8.5 创新性主张的边界

我们不主张 FiLM、掩码训练或种子集成本身新颖。**主张的是：在一个已被证明"未见化合物信息不可得"的任务上，把"如何优雅地不知道"当作一等设计目标，并给出可测量、可证伪的实现。** 上面每条设计都挂着一个先于它做出的测量，以及一个把它证伪的对照。种子集成与契约固化属于工程卫生，归入第十一节，不计入创新点。

## 九、结果（官方六模块口径）

### 9.1 冻结验证集

（对照池 `train | val`，μ_ctx 用官方粒度；匹配行 2,806 = seen 139 / S1 1,065 / S2 1,333 / S3 269）

| 官方模块 | 权重 | 本方案 | 平凡基线 | 倍数 |
|---|---:|---:|---:|---:|
| 绝对保真度 sample-PCC | 20% | **0.988598** | 0.953882（train 均值） | — |
| 匹配对照 FC（全体） | 25% | **0.491355** | — | — |
| 上下文均值残差 S1 | 20% | 0.203387 | — | 见第四节 |
| 药物均值残差 S2 | 20% | 0.407705 | — | 单聚类，仅描述性 |
| 时间插值 FC | 10% | **0.671725** | 0.2291 | **2.9×** |
| 高效应 DEP 方向准确率 | 5% | **0.891936** | 0.7285 | — |
| 官方加权 W | — | **0.5218** | — | — |

**在化合物信息确实可用的场景上，本方案远优于平凡基线**：时间插值 FC 0.6717 是平凡基线 0.2291 的 2.9 倍，且是全部场景中最高的一项；DEP 方向准确率 0.8919 对 0.7285，高效应 PCC 0.7496。这两个模块合计 15% 的权重此前从未被任何一轮 gate 评估过，本项目首次给出实测，结论是**失分不在这里**。

### 9.2 与经典基线家族的对照

我们实现并评测了官方基线家族的三种构造，判据在数据进入前冻结（`configs/p05_gates.json`）：

| 候选 | W | ΔW |
|---|---:|---:|
| 本方案 | **0.5218** | — |
| 分场景低秩统计 | 0.4471 | −0.0747 |
| 分场景梯度提升 | 0.4424 | −0.0794 |
| 全局梯度提升（PCA-128） | 0.4186 | −0.1032 |
| 全局梯度提升（PCA-64） | 0.4181 | −0.1037 |

采纳门槛 ΔW ≥ +0.010，实测为反方向的 7.5×~10.4×；**六个加权模块无一例外全部更差**，S1 的四个差值经配对聚类 bootstrap 全部 CI 不含 0。

**限定**：这三种构造在一天内实现，梯度提升迭代上限 25/20，其中两种夹了 PCA 瓶颈。可主张的是"**这三种具体构造被决定性劣于**"，而非"梯度提升这条路走不通"。

### 9.3 提交产物（train-only 口径）

最终提交仅在 `split_final == "train"` 的 5,920 行上重训。测试集 post-hoc（预测冻结后一次性读取，不回流任何选择）：逐样本 PCC `0.986936`、R² `0.972417`、log2 RMSE `0.448899`、MAE `0.297412`；matched-control FC PCC `0.471158`，对照匹配覆盖率 100%。口径敏感性披露：若采用仅测试集内部的对照池，覆盖率 49.20%、FC PCC 为 `0.404327`；两种口径下三种子集成均优于单种子基线，结论不随口径改变。

## 十、证伪阶梯

每条分支都在数据进入前写死判据，失败即停，阈值从未事后移动。

| # | 分支族 | 代表实测 | 裁决 |
|---:|---|---|---|
| 1 | 残差公式建模（P2） | DirectRidge 0.984998 / 0.379957 vs ResidualRidge 0.963658 / 0.254537 | 拒绝 |
| 2 | 化学结构特征（P3 / C1 / C1R） | 官方 6 个未见化合物 ΔRMSE **+0.097757**；严格交叉拟合残差上 +0.000329487，100 次置换 p = 1.0 | 拒绝，真实结构不优于随机投影 |
| 3 | 基因组特征（P4 / G1 / G1R） | genome kernel LOSO macro ΔRMSE **+0.025328**；关联成立（presence +0.421924, p=.000999）但未转化为预测 | 关联 ≠ 预测，关闭 |
| 4 | 外部蛋白组迁移（CABT / RC-EPT / LR-EPT） | 三次独立尝试均在开发 OOF 有信号、冻结实体上失效；LR-EPT 最差菌株 CGD **+0.087488** | 根因：仅 3 个精确训练菌株，迁移系数不可识别 |
| 5 | 逐蛋白后处理校正（C 权重 / plate 校准 / EB 偏移） | EB 逐蛋白校准 ΔRMSE **+0.004050**，三子集全退化 | 四次独立不可迁移，家族级关闭 |
| 6 | 指标感知损失与低秩解码器（P5.5 / P6） | 0.985635 / 0.388903 与 0.985227 / 0.382296，均低于直接输出头 | 拒绝 |
| 7 | 稀疏机制先验（M3 / SSPS） | Gate-0 ρ̄ = 0.0059（CI 含 0）；**同支撑集符号最优上限 0.0491 < 地板 0.053** | 数学上关闭，与先验质量无关 |
| 8 | 稠密表征通道（DCB-40） | 主通道 0.0268（maxT 后不显著）；稠密−稀疏四通道全负 | INDETERMINATE，按预注册**明确不作为贡献** |
| 9 | 优化侧杠杆（SWA / 多 seed / RAP-R4） | 单项最优 ΔW +0.0075；在后来标定出的 +0.002 地板下部分可过，但见第 14 行 | NOT ADOPTED |
| 10 | 经典基线家族（P0.5） | ΔW −0.0747 ~ −0.1037 | DOMINATED |
| 11 | 零信息候选（R1 凸混合） | ΔS1 +0.02191，CI [+0.00797, +0.03853]；但纯种子噪声可产出 CI [+0.00497, +0.03696]（见 §7.1），且化合物置换 null 与真实分数逐位相同 | **不采纳**：零信息，效应量在噪声内 |
| 12 | 容量 / EMA / 跨架构混合（P3） | 十候选最大 ΔW +0.0050，单项均未过当时地板；记录见 `reports/P3_CAPACITY_EMA_BLEND_20260827.md` | NOT ADOPTED |
| 13 | 低速率菌株掩码（P4 / R-C） | 0.05 → RMSE +0.0037、0.10 → +0.0119，随速率单调恶化 | 假说被证伪：不是"0.25 太狠"，方向本身不成立 |
| 14 | 跨家族组合（P4 / R-A，ep120+EMA+10选3） | 在验证实验链上通过全部五条采纳条件（ΔW +0.00788，六模块全改善）；但同一配置、同样三个种子换到提交链后**配置效应翻符号**（−0.001339 → +0.002174） | **采纳后被推翻**：跨族翻符号 + 十种子配对 3/10 改善（p = 0.344），确证为抽样假象 |
| 15 | 稠密先验重开（P2 / R2-000） | 沿用 3× 保守安全系数下，硬帽内无 δ 达到 0.80 功效（δ=0.20 时 P = 0.740） | UNDERPOWERED，**明确不作为贡献**，未消耗任何新数据源 |

三条自证记录：

- DCB-40 中若把 δ 定为 0.10，本轮结果会满足 REPRESENTATION GAP 的全部条件；但 δ 在看到任何 ρ 之前已冻结为 0.05，**如实判 INDETERMINATE，未事后调整**。
- R1 凸混合是八个候选中唯一改善 20% 权重模块的，配对 bootstrap CI 不含 0、双粒度同号、其余模块不退化，**仍按 0.053 地板判 FAIL**。该决定后来被证明是对的，见 §7.1。
- P4 的 R-A 候选**通过了全部五条冻结采纳条件**（ΔW +0.00788、六个模块全部改善、Gate-Z 干净），我们在采纳前做了一次不在预注册清单里的跨种子族复核，结果推翻了采纳；随后的十种子配对比较（3/10 改善，配对差均值 +0.001418 即更差，符号检验 p = 0.344）把它确证为抽样假象。详见 §7.2。

### 7.1 一条被自己推翻的显著性证据

S1 的配对聚类 bootstrap 只有 6 个聚类，**假阳性率可观**。实测：一个**什么都没改、只换种子**的同配置
3-seed 集成给出 ΔS1 = +0.016275、CI [+0.004972, +0.036964]，**同样排除 0**——与 R1 w010 的
CI [+0.007973, +0.038528] 几乎重合，而 R1 w005 的效应量（+0.012145）甚至小于这一抽纯噪声。

→ **"CI 不含 0" 不能作为 S1 上效应为真的证据。** 这反过来说明 0.053 效应量地板是必要的：
它拦下了一个显著性检验误判为显著的候选。

### 7.2 一个通过了全部条件却仍被否决的候选

P4 的 R-A（120 轮 + EMA 0.99 + 十选三种子）在验证实验链上通过全部五条采纳条件：
ΔW +0.00788（同配置换种子噪声上限 0.0015419 的 5.1 倍）、ΔW_noS2 +0.00729、**六个模块全部改善无一变差**、
Gate-Z 不触发（S1 仅占 ΔW 的 30.9%）、Gate-R1F 不适用。验证 RMSE 0.417717、FC 0.503782 均为项目最佳。

采纳前的常规复核推翻了它。项目里有两条并存且都早于本轮的 fit-seed 约定——验证实验链
`fit_seed = seed + 9000`，提交链 `fit_seed = seed`。把**同一配置、同样三个固定种子**放到两条链上：

| | ep40 基线 | ep120 + EMA | 配置效应 |
|---|---:|---:|---:|
| 验证实验链 | 0.420258878 | 0.418919577 | **−0.001339（更好）** |
| 提交链 | 0.421007924 | 0.423181721 | **+0.002174（更差）** |

**同一配置、同样种子，只换 fit_seed 偏移，配置效应翻符号。** 因此 +0.00788 不是配置的属性，
而是一个未被测量的自由度的属性。按选出的三个种子在提交链上重训，验证 RMSE 为 0.428203895，
比该链基线差 +0.0072——那三个种子是为另一个种子族选出来的。

再用**已有产物**做一次零成本的配对检验：同一种子族下，`outputs/p1_multiseed/`（ep40）与
`outputs/p4_ra/`（ep120+EMA）本就覆盖同样十个种子，按种子配对逐一比较：

| 统计量 | 值 |
|---|---:|
| 改善的种子数 | **3 / 10** |
| 配对差均值 | **+0.001418（ep120+EMA 更差）** |
| 配对差标准误 | 0.000964 |
| 配对 t（df = 9） | +1.470（不显著） |
| 符号检验 p | 0.344 |
| 单种子均值 | ep40 0.428870 ｜ ep120+EMA 0.430287 |

**方向本身就是错的**：ep120+EMA 平均更差，t 检验与符号检验均不显著。这张表还揭示了选择机制——
seed 7 在 ep40 下排第 8/10，在 ep120+EMA 下排第 3/10，是唯一大幅改善的那一个；十选三恰好选中它，
于是"配置效应"与"该种子在该配置下的运气"被绑死无法分离。之前测得的 −0.001339，本质是十个种子里
靠前那三个的抽样运气，十选三又叠了第二层选择偏倚。**两层都是随机性，不是配置。**
逐种子明细见 `reports/P4_FINAL_LEVERS_20260830.md`。

**推论（本轮最重要的方法学结论）**：检测地板必须跨越**所有**未被测量的自由度，而不只是你恰好变动的那一个。
我们标定的 +0.002 地板来自单一种子族内的 null，跨族效应与它同量级，所以那个地板是乐观的。
完整记录见 `reports/P4_FINAL_LEVERS_20260830.md`，被否决的配置与理由记录在 `configs/final.yaml`
的 `rejected_alternatives`。

## 十一、数据使用与防泄漏原则

本节严格对应官方赛题说明「数据使用约束与最终评测」条款：*训练仅可使用 train 划分的蛋白质组标签，验证集用于模型选择；验证集与测试集均不得参与训练，也不得用于估计任何统计量（含保留蛋白列表与归一化参数）*。

- **训练范围**：最终提交模型只在 `split_final == "train"` 的 5,920 行上训练。`val_strain_only` / `val_chem_only` / `val_both` / `val_time` 合计 3,038 行只用于模型选择时的指标计算，不进入任何一次参数更新。
- **统计量范围**：下列全部量均只由 `train` 划分拟合——蛋白保留列表（缺失率 < 0.8）、六个类别字段的映射表、温度/时间的标准化参数与 RBF 中心、目标 `mean`/`std`、821 个未建模蛋白的填充均值。脚本内置硬断言，任何非 train 行进入拟合索引即中止。
- **测试数据**：测试蛋白组真值不参与训练、特征选择、标准化参数拟合或模型选择；最终提交脚本只读取 `train_val` 元数据与蛋白标签、以及测试元数据，从不打开 `proteome_test`。第 9.3 节的测试自评指标在预测文件冻结并通过契约校验之后才计算。
- **判据纪律**：每个阶段的判据在真实数据进入管线之前写入配置并记录 SHA-256；执行期未移动任何阈值。
- **两条 fit-seed 约定**：验证实验链（`scripts/a2b_train_variants.py`）用 `fit_seed = seed + 9000`，
  提交链（`scripts/train.py`）用 `fit_seed = seed`。两者都早于本轮，且历轮的选择证据与提交产物一直分属两条链。
  因此**验证集上的数字支持的是配置，不是提交产物的那三组具体权重**；本文第 9.1 节（验证）与第 9.3 节
  （测试 post-hoc）据此分开报告。§7.2 记录了这个区分在一次候选采纳中起到的决定性作用。
- 输出蛋白列严格按官方蛋白特征契约排列；所有随机过程固定种子，GPU 运行时关闭 cuDNN benchmark 并启用确定性设置。
- 上述边界可由 `python scripts/validate_submission.py --prediction prediction.csv --run-dir runs/final` 独立复核（无需重跑训练）；`runs/final/artifacts/preprocess_contract.json` 记录 `fit_rows = 5920` / `held_out_val_rows = 3038`，`train.py` 内置硬断言使任何非 train 行进入拟合索引即中止。
- **外部资源披露**：所使用公开数据与模型的来源、版本、许可、覆盖率与弃权规则见 `external_data/RAW_SOURCES.md`。

## 十二、可复用工具、复现与开源边界

### 12.1 四件可被社区直接接续的工具

1. **官方口径六模块评估器**（`scripts/evaluate_official_modules.py`）。实现官方对照池、按"实体是否在训练划分中"分层、四种 μ_ctx 粒度、化合物置换 null、配对聚类 bootstrap。**任何参赛队都可以用它自查第五节那个 57% 的对照池问题。** 实现上有一个值得记录的坑：μ 自身在"该上下文所有训练行都缺测"的蛋白上是 NaN，减去 μ 之后**必须重算观测掩码**，否则整行被判无效，S1 会被低估为 0.143 而不是 0.203。
2. **化合物不变性检查**（`scripts/compound_invariance_check.py`）。第四节诊断的通用实现，替代零功效的置换检验。任何新候选都应先过这一关再谈 S1 分数。
3. **嵌套无泄漏 LOCO harness**（`scripts/dcb_loco_harness.py`）。37 折留一化合物，逐折泄漏断言，**用独立实现逐位复现参考值 0.005872 / −0.000727 / 0.4636**。
4. **交叉拟合 OOF 基础设施与泄漏审计**。compound / strain / both / time / plate 五类折叠。我们在此发现并修复了一个历史 both-fold 泄漏 bug（CSV writer 忽略了 train 掩码），修复后通过 **5,920/5,920 恰好一次覆盖**与双实体排他性检验。

### 12.2 开源边界

官方原始数据为非公开资料，不随代码包分发。代码、配置、冻结判据、评估器与审计脚本可公开；外部公开数据的来源与版本已列明，可由第三方按同样口径重建。

## 十三、局限性

- **扰动效应的预测能力仍然偏弱，这是本方案最大的实质局限。** 官方 FC 口径下 Δ_pred 与 Δ_true 共享 −y_control 项，因此基线预测准确的模型即可获得可观的 FC PCC；剥离该共享项后，纯粹的化合物特异效应仅 0.175（已见化合物）与 0.042（未见化合物）。占官方权重 65% 的三个模块（FC 25% + 上下文均值残差 20% + 药物均值残差 20%）考察的正是这一能力。第四与第七节给出了这一局限的定量边界与突破所需条件，但**我们没有突破它**。
- 20% 权重的 S2 模块，当前划分不支持模型选择（第六节），我们只作描述性报告。
- 第七节的边界以留出训练化合物测得，而那是训练化学空间内的插值；对真实的未见化合物任务，我们给出的数仍然偏乐观。
- 为严格遵守官方数据使用约束，最终模型只使用 5,920 行训练，未使用验证划分的 3,038 行（占 `train_val` 的 33.9%）。这在样本量上有明确代价，但保证复现核验口径干净。
- 官方数据包未提供 `strain_id → accession` 映射。开发期曾从 1011 Yeast Genomes 公开 supplementary table 得到候选对齐（`reports/ACCESSION_MAPPING_AUDIT.md`），但缺少样本级 provenance 证据，基于它的探索性实验也未取得稳定收益，因此最终模型不引入任何外部基因组特征。
- 公开资料中发现了与虚拟酵母研究方向相关的项目线索，但尚未形成当前竞赛样本级的可验证映射。
- 化学结构特征与额外菌株特征的探索性实验未形成稳定可复现的收益，未进入最终候选模型。
- 本地评价结果不能替代官方隐藏评测。
